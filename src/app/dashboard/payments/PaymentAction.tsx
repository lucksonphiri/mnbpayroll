"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function PaymentAction({id}:{id:string}){const r=useRouter();const [busy,setBusy]=useState(false);async function mark(){const reference=prompt("Enter payment reference:")||"";if(!reference)return;setBusy(true);await fetch("/api/payments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({employeePayrollId:id,status:"paid",method:"bank",reference,paymentDate:new Date().toISOString().slice(0,10)})});setBusy(false);r.refresh();}return <button disabled={busy} onClick={mark} className="rounded-lg bg-green-700 px-3 py-2 text-sm font-bold text-white">{busy?"Saving...":"Mark Paid"}</button>}
