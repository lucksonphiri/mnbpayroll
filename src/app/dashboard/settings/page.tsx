import SettingsForm from "./SettingsForm";import {requireRole} from "@/lib/auth";import {sql} from "@/lib/db";export const dynamic="force-dynamic";export default async function Page(){await requireRole(["Administrator"]);const rows=await sql`SELECT setting_key,setting_value FROM system_settings`;const initial=Object.fromEntries(rows.map((r:any)=>[r.setting_key,String(r.setting_value??"")]));return <div className="space-y-8"><div><h1 className="text-3xl font-black">System Settings</h1><p className="mt-2 text-slate-500">Organisation and payroll defaults.</p></div><SettingsForm initial={initial}/></div>}
