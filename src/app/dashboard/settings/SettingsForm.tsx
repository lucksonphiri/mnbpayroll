"use client";
import { FormEvent,useState } from "react";
export default function SettingsForm({initial}:{initial:Record<string,string>}){const [form,setForm]=useState(initial);const [message,setMessage]=useState("");async function save(e:FormEvent){e.preventDefault();const r=await fetch("/api/settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();setMessage(d.message);}const fields=[['company_name','Company name'],['company_address','Address'],['company_email','Email'],['company_phone','Phone'],['default_currency','Default currency'],['employee_number_prefix','Employee number prefix']];return <form onSubmit={save} className="rounded-2xl bg-white p-6 shadow-sm"><div className="grid gap-5 md:grid-cols-2">{fields.map(([k,l])=><label key={k} className="text-sm font-semibold">{l}<input value={form[k]||""} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-2 w-full rounded-xl border px-4 py-3"/></label>)}</div><button className="mt-6 rounded-xl bg-blue-700 px-6 py-3 font-bold text-white">Save Settings</button>{message&&<p className="mt-4 text-green-700">{message}</p>}</form>}
